# processos para rodar o projeto sensores

- instalar docker e docker compose no ambiente
- compilar o projeto com `docker build -t sensores .`
- iniciar o conjunto de containers com `docker-compose up`
